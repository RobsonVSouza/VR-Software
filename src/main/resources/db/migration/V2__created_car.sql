CREATE TABLE car (
id int8 PRIMARY KEY,
name varchar(250),
description varchar(250),
daily_rate varchar(250),
available varchar(250),
license_plate varchar(250),
brand_id varchar(250),
category_id varchar(250),
color varchar(250),
created_at timestamp not null
);